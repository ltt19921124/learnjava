	数组
	
	数组静态初始化：
	静态初始化是指由程序员自己为数组对象的每个元素赋值，由系统自动计算出数组的长度。
	
	数组动态初始化：
	动态初始化是指由程序员自己指定数组对象的长度，由系统先自动为其赋值。程序中程序员可以为元素重新赋值。
	
	整形数组元素默认初始值是0，
	
	java 5以后提供了foreach循环
	
	没有多维数组，
	数组是引用类型，数组变量实际上是一个引用，引用指向实际的内存